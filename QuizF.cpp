#include <bits/stdc++.h>
using namespace std;
int main () {
    string s;
    cin >> s;
    vector <char> t;
    for(int i = 0; i < s.size(); i++) {
        if(s[i] >= 'a' && s[i] <= 'z') {
            cout << s[i];
        }
        
    }
  
}